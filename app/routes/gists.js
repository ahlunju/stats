import Ember from 'ember';

export default Ember.Route.extend({
  // model: function() {
  //   var parentModel = this.modelFor('statistic');
  //   return Ember.$.getJSON(parentModel.gists);
  // }
});
